import React, { useState, useEffect } from "react";
import Axios from "axios";

function Appoinment() {
  return (
    <div>
      <h1>Appoinment</h1>
      {/* <ul>
        {Appoinments.map((Appoinment) => (
          <li key={Appoinment.name}>{Appoinment.contactNumber}</li>
        ))}
      </ul> */}
    </div>
  );
}

export default Appoinment;
